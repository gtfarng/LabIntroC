#include<stdio.h>
int main()
{ 
  float total,cal,price;
  printf("Enter Total sales: ");
  scanf("%f",&total);
  
  if(tatal<1000)
  {  
     cal=total*0.05;
     price =cal+total;
  }
  else if total>=1000&&total<=5000)
   { 
      cal=total*0.07;
      price=cal+total; 
   }
  else if(total>5000)
  {
     cal=total*0.1;
     price=cal+total;
  }
   
          printf("Total sales = %.2f and Sales commission = %.2f\n",total,cal);



  return 0;
}
